[supervisord]
nodaemon=true

[program:telegraf]
command= sh -c "/usr/bin/telegraf && kill -s SIGTERM $(cat supervisord.pid)"

[program:php-fpm7]
command= sh -c "/usr/sbin/php-fpm7 -F && kill -s SIGTERM $(cat supervisord.pid)"

[program:nginx]
command= sh -c "nginx -g 'daemon off;' && kill -s SIGTERM $(cat supervisord.pid)"